# frozen_string_literal: true

RSpec.describe Jupiter::Ag::Swap do
  it "has a version number" do
    expect(Jupiter::Ag::Swap::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
